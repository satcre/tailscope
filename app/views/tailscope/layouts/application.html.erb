<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Tailscope</title>
  <%= csrf_meta_tags %>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    .code-highlight { background-color: #fef3c7; }
    pre code { font-size: 0.875rem; line-height: 1.5; }
  </style>
</head>
<body class="bg-gray-50 min-h-screen">
  <nav class="bg-gray-900 text-white">
    <div class="max-w-7xl mx-auto px-4 py-3 flex items-center gap-8">
      <a href="<%= tailscope.root_path %>" class="text-lg font-bold tracking-tight">Tailscope</a>
      <div class="flex gap-4 text-sm">
        <a href="<%= tailscope.root_path %>" class="hover:text-gray-300">Issues</a>
        <a href="<%= tailscope.queries_path %>" class="hover:text-gray-300">Queries</a>
        <a href="<%= tailscope.requests_path %>" class="hover:text-gray-300">Requests</a>
        <a href="<%= tailscope.errors_path %>" class="hover:text-gray-300">Errors</a>
        <% if Tailscope::Debugger.enabled? %>
          <a href="<%= tailscope.debugger_index_path %>" class="hover:text-yellow-200 text-yellow-400">Debugger</a>
        <% end %>
      </div>
    </div>
  </nav>

  <main class="max-w-7xl mx-auto px-4 py-6">
    <%= yield %>
  </main>

  <footer class="max-w-7xl mx-auto px-4 py-4 text-center text-xs text-gray-400">
    Tailscope v<%= Tailscope::VERSION %>
  </footer>
</body>
</html>
